namespace ReflowOven.API.Integration.Peripheral
{
    public class RPiProgram
    {
        // Executar essa thread à cada 1s, chama-la no BackgroundWorkerService

        // Enviar pela keep alive pela serial somente para dizer se a placa esta viva

        // Inserir no banco de dados os dados recebidos pela serial e colocar o horário

        // Blink no led de status do RPi


    }
}
